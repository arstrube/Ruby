require './libc'

describe "Using Libc" do
  context "memory" do
    it "can be allocated"
    it "can be freed"
  end
  context "a file" do
    it "can be opened for reading" 
    it "can be opened for writing"
    it "can be read"
    it "can be closed"
    it "can be written"
  end
end
